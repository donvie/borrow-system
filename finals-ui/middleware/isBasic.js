export default function(context) {
    // if (context.$auth.$state.user !== null) {
    //     if (context.$auth.$state.user.user_role !== 'basic') {
    //         context.redirect(`/`);
    //     } 
    // }
    // else {
    //     console.log('user empty')
    // }
}